<?xml version="1.0" encoding="UTF-8" ?>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:py="http://genshi.edgewall.org/"
      xmlns:xi="http://www.w3.org/2001/XInclude"
      lang="en-AU">
<xi:include href="_commonwrapper.xml" />

<?python
from viewutils import parsed_content, relative_url
article = node
issue = article['dc:isPartOf']
journal = issue['mhs:isIssueOf']
?>

<head>
    <title>${Markup(article['dc:title']).striptags()} - ${journal['dc:title']} Vol. ${issue['mhs:volume']}, Issue ${issue['mhs:issueNumber']}</title>
    <link rel="stylesheet" type="text/css" href="/style/article.css" />
    <script type="text/javascript" src="${req.script_name}/script/jquery-1.3.2.js" />
    <script type="text/javascript" src="${req.script_name}/script/jquery.scrollTo-1.4.1.js" />
    <script type="text/javascript" src="${req.script_name}/script/footnote.js" />
</head>

<body>

${breadcrumbs([('/journals/', 'Journals'), 
               (relative_url(journal.uri), journal['dc:title']),
               (relative_url(issue.uri), 'Vol. %s, Issue %s' % (issue['mhs:volume'], issue['mhs:issueNumber']))])}

<h2>${article['dc:title']}<abbr class="unapi-id" title="${article.uri}" /></h2>

<div class="metabox-container">
    <div class="article-meta metabox">
        <h4>Article details</h4>
        <p>Published in <a href="${relative_url(issue.uri)}">Vol. ${issue['mhs:volume']}, Issue ${issue['mhs:issueNumber']}</a> of<br /><a href="${relative_url(journal.uri)}"><em>${journal['dc:title']}</em></a></p>
        <p class="other-formats"><a href="${relative_url(article.uri)}.pdf">Original print version</a></p>
        <div class="subjects">
            <p>Subjects:</p>
            <ul py:with="subjects = sorted(article.getall('dc:subject'), key=lambda s: s['mhs:label'])">
                <li py:for="subject in subjects">
                    ${subject['mhs:label'].replace('--', ' &ndash; ')}
                </li>
            </ul>
        </div>
        <p class="metadata-links">Metadata:
            <a href="${relative_url(article.uri)}.nt">RDF</a>, 
            <a href="${relative_url(article.uri)}.bib">BibTeX</a>, 
            <a href="${relative_url(article.uri)}.mods">MODS</a>, 
            <a href="${relative_url(article.uri)}.end">Endnote</a>
        </p>
    </div>
</div>

<p py:with="author = article['dc:creator']">
    by <a href="${relative_url(author.uri)}">${author['foaf:name']}</a>
</p>

${parsed_content(req.content_dir + relative_url(article.uri) + '.html', article.uri)}

</body>
</html>
