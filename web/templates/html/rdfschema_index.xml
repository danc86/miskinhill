<?xml version="1.0" encoding="UTF-8" ?>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:py="http://genshi.edgewall.org/"
      xmlns:xi="http://www.w3.org/2001/XInclude"
      lang="en-AU">
<xi:include href="_commonwrapper.xml" />

<head>
    <title>RDF Schema</title>
</head>

<body>

<h2>RDF Schema</h2>

<h3>Classes</h3>

<ul>
    <li py:for="cls in sorted(classes, key=lambda c: c.uri)">
        <a href="${cls.uri}">${unicode(cls.uri).split('/')[-1]}</a>
    </li>
</ul>

<h3>Properties</h3>

<ul>
    <li py:for="property in sorted(properties, key=lambda p: p.uri)">
        <a href="${property.uri}">${unicode(property.uri).split('/')[-1]}</a>
    </li>
</ul>

</body>
</html>
