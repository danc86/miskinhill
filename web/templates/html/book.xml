<?xml version="1.0" encoding="UTF-8" ?>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:py="http://genshi.edgewall.org/"
      xmlns:xi="http://www.w3.org/2001/XInclude"
      lang="en-AU">
<xi:include href="_commonwrapper.xml" />
<xi:include href="_bookinfo.xml" />

<?python
import representations
from viewutils import striptags
book = node
?>

<head>
    <title>${striptags(book['dc:title'])}</title>
    <py:for each="r in representations.for_node(node)" py:if="r.format != 'html'">
        ${r(req, node).link()}
    </py:for>
    <link rel="stylesheet" type="text/css" href="/style/cited.css" />
</head>

<body>

<h2>Book<abbr class="unapi-id" title="${book.uri}" /></h2>

${bookinfo(book)}

<h3 class="cited-in">Cited in</h3>
<ul class="articles">
    <li py:for="article in sorted(set(c['dc:isPartOf'] for c in book.reflexive('mhs:cites')), key=lambda a: a['dc:isPartOf']['mhs:publicationDate'], reverse=True)"
        py:with="issue = article['dc:isPartOf']; journal = issue['mhs:isIssueOf']">
        <div class="article"><a href="${article.uri}">${article['dc:title']}</a></div>
        <div class="issue">in <a href="${issue.uri}"><em>${journal['dc:title']}</em> Vol. ${issue['mhs:volume']}, Issue ${issue['mhs:issueNumber']} (${issue['dc:coverage']})</a></div>
    </li>
</ul>

</body>
</html>
